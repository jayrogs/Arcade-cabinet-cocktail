# Flycast (Dreamcast and NAOMI) on this Pi: built from source for OpenGL ES, the Pi 4s
# mature graphics driver (the Vulkan one made the on-screen display flicker every other
# frame). Sound through PipeWire: the usual output crashes when a NAOMI game changes its
# sound speed.
audio_driver = "pipewire"
